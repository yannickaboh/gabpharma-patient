# Gab'Pharma Patient — suivi du branchement API réelle

**Créé le :** 13 août 2026
**But :** suivre, module par module, le passage du mode démonstration (données locales statiques) au vrai backend Django (`/api/v1/...`). Cocher au fur et à mesure, ne pas re-décider l'ordre sans raison — voir note en bas si l'ordre doit changer.

## Déjà branché et vérifié (13 août 2026, sur S8 physique + backend local `:8004`)

- [x] Connexion (`POST /mobile/auth/login/`)
- [x] Vérification 2FA (`POST /mobile/auth/verify-2fa/`)
- [x] Restauration de session (`GET /mobile/auth/me/`) — testé en relançant l'app après connexion, session persistée via `flutter_secure_storage`
- [ ] Renvoi du code 2FA (`POST /mobile/auth/resend-2fa/`) — câblé dans le code (`AuthSession.resendTwoFactor`) mais jamais testé sur device

**Bug corrigé au passage** : `api_client.dart` ne fixait jamais `Content-Length`, donc `dart:io` basculait en `Transfer-Encoding: chunked` — incompatible avec le serveur de dev Django (`manage.py runserver`, wsgiref), qui rejetait toute requête POST/PUT réelle (`Bad request syntax`). Fix commité (`99bcf34`) : encoder le corps en bytes UTF-8 et fixer `contentLength` explicitement avant `request.add(...)`.

**Gap connu, non corrigé côté app (mais débloqué côté backend le 23 août 2026)** : le token d'accès JWT expire au bout de 20 min (`SIMPLE_JWT.ACCESS_TOKEN_LIFETIME`). Jusqu'au 23 août 2026, **aucun endpoint `/mobile/auth/refresh/` n'existait côté Django** pour le rafraîchir (vérifié le 18 août 2026, `grep` vide dans `apps/api/urls.py`). **`POST /mobile/auth/refresh/` existe désormais** (wrapper autour de `TokenRefreshView` de `djangorestframework-simplejwt`, revérifie rôle/statut du compte à chaque refresh — rejette avec 403 si le compte n'est plus actif entre-temps). Rien n'a changé côté app : le fix du 18 août (déconnexion propre sur 401 plutôt qu'écran mort, `ApiClient.onUnauthorized`) reste en place et fonctionne toujours, mais l'app n'appelle pas encore `/refresh/` avant de couper la session — **reste à faire côté Flutter** : intercepter le 401, tenter un refresh silencieux avec le `refresh` token déjà stocké dans `flutter_secure_storage` (récupéré mais jamais utilisé jusqu'ici), et ne déconnecter que si le refresh échoue aussi.

## Ordre convenu pour la suite

1. [x] **Accueil** — `GET /mobile/patient/summary/` (profil, panier, commande active, commandes récentes, notifications, favoris, offres). Vérifié sur S8 le 17 août 2026 : prénom réel, badge panier masqué si vide, bannière "commande en cours" masquée si aucune commande active (pas de fausse donnée), pharmacies/produits/catégories dérivés de `featured_stocks` (dédupliqués), statut pharmacie réel (Ouvert 24h/24 / Ouvert / Fermé selon `is_24_7`/`is_on_duty`). Bug corrigé au passage : overflow de 49px sur les cartes produits (`GridView childAspectRatio` 0.72 trop serré → 0.58). Bouton "Ajouter" (home) et détail médicament/pharmacie restent sur les données démo — hors scope de ce module (voir étapes 2-3).
2. [x] **Recherche + catalogue** — `GET /mobile/patient/catalog/categories/`, `GET /mobile/patient/zones/`, `GET /mobile/patient/catalog/` (recherche texte avec debounce 400ms, filtre zone, filtre catégorie via bottom sheet, pagination "Charger plus"). Vérifié sur S8 le 18 août 2026 : zones réelles (Libreville/Owendo/Akanda/Bikélé), résultats réels avec comptage exact, recherche texte fonctionnelle server-side, combinaison recherche+zone testée. Filtre "Forme" reste honnêtement indisponible (l'API ne le supporte pas). Boutons "Ajouter"/"Alerte" et détail médicament restent sur données démo — hors scope (voir étapes 3 et 5). **Mise à jour du 23 août 2026** : le filtre Forme est désormais disponible côté backend (`GET /mobile/patient/catalog/forms/` pour la liste des valeurs, `?form=<code>` sur `/catalog/`) — pas encore branché côté app, l'UI de recherche n'a pas de sélecteur Forme à ce jour.
3. [x] **Détail médicament / pharmacie** — `GET /mobile/patient/catalog/stocks/<id>/`, `GET /mobile/patient/pharmacies/<id>/`, `GET /mobile/patient/pharmacies/<id>/catalog/`. Vérifié sur S8 le 18 août 2026. Détail médicament : id du stock transmis via `Navigator` depuis Accueil/Recherche/détail pharmacie (`ModalRoute.settings.arguments`), "Pharmacies Disponibles" reconstitué en interrogeant `/catalog/?q=<nom du médicament>` (pas d'endpoint dédié "médicament tous pharmacies", approximation assumée et honnête). Détail pharmacie : champs vides (adresse/téléphone/horaires/services) affichés avec un texte honnête plutôt qu'une valeur inventée — confirmé avec la Pharmacie du Centre qui a tous ces champs vides en démo. Distance factice ("850m", "1.2 km") supprimée partout (pas de géolocalisation). Panier/Favoris toujours honnêtement "pas encore connecté" — hors scope (étapes 4-5). **Mise à jour du 23 août 2026** : `latitude`/`longitude` sont désormais exposés sur les pharmacies, et l'API accepte `?lat=&lng=` sur `/catalog/`, `/catalog/stocks/<id>/`, `/pharmacies/` et `/pharmacies/<id>/` pour calculer une vraie `distance_km` (Haversine côté serveur, arrondie au dixième de km) et trier la liste des pharmacies par proximité. **Mise à jour du 23 août 2026 (suite, même jour)** : SDK `google_maps_flutter` intégré et branché sur l'écran Détail pharmacie — aperçu de carte statique (`liteModeEnabled`) avec un vrai marker sur les coordonnées de la pharmacie, bouton "Navigation" ouvrant une vraie app de navigation externe (`google.com/maps/dir`) au lieu du message "indisponible en démonstration" (voir `CLAUDE.md`, entrée "SDK Google Maps"). Pharmacie du Centre entièrement re-seedée pour les tests (adresse, téléphone, horaires, services, coordonnées GPS réelles). Reste non branché : la géolocalisation du patient elle-même (permission + package Flutter, ex. `geolocator`) pour trier la recherche/liste de pharmacies par proximité — l'API sait déjà calculer la distance, il manque la position patient côté app.
4. [x] **Favoris** — `GET/POST /mobile/patient/favorites/`, `DELETE /mobile/patient/favorites/<id>/`. Vérifié sur S8 le 18 août 2026 : liste réelle (prix, statut stock, dosage/forme), suppression réelle avec undo (ré-appelle `POST /favorites/`), état vide honnête. Bouton cœur toggle sur `MedicationDetailScreen` branché en prime (ajout/retrait réels — nécessite un aller-retour par `GET /favorites/` pour retrouver l'id du favori avant suppression, l'API ne l'expose pas ailleurs). Case "rupture de stock" (`offer_count == 0`) désactive la carte plutôt que de planter sur un prix nul. Panier reste honnêtement "pas encore connecté" (étape 5).
5. [x] **Panier** — `GET /mobile/patient/cart/`, `POST/PATCH/DELETE /mobile/patient/cart/items/...`, `POST /mobile/patient/cart/clear/`. Vérifié sur S8 le 19-20 août 2026 : ajout/modif quantité/suppression/vidage tous réels, panier persiste bien côté serveur (retrouvé intact le lendemain), résumé honnête ("frais de livraison calculés à l'étape suivante" au lieu d'un montant fixe inventé), bouton "Passer à la commande" désactivé si `is_valid_for_checkout` est faux. Tous les boutons "Ajouter" de l'app (Accueil, Recherche, détail médicament, détail pharmacie, Favoris) branchés sur `addToCartWithFeedback` (nouveau helper partagé dans `widgets.dart`). Nouveau `ValueNotifier<int> cartUpdates` (`patient_catalog.dart`) synchronise le badge de la nav bar et l'écran Panier entre eux sans dépendre d'un rechargement manuel — bumpé par chaque mutation réussie du panier, quel que soit l'écran d'origine. CheckoutScreen (route `/checkout`) reste demo — c'est l'étape 6.
6. [x] **Checkout** — `GET /mobile/patient/zones/`, `GET /mobile/patient/payment-methods/`, `POST /mobile/patient/checkout/`. Vérifié sur S8 le 21 août 2026 : zones et moyens de paiement réels chargés depuis l'API (plus de communes/2 modes de paiement codés en dur), adresse+zone obligatoires seulement en mode livraison (retrait exempté). Récapitulatif honnête : frais de livraison affichés "calculés à la confirmation" plutôt qu'un montant inventé (l'API ne les expose qu'après la commande, pas de simulation côté client). Deux chemins testés et confirmés en base : paiement à la livraison (COD) → commande créée avec `payment_status=unpaid`, écran "Commande confirmée" (`CMD-00029`, Pharmacie du Centre, retrait, 500 FCFA) ; moyen de paiement en ligne (Airtel Money) → commande créée avec `payment_status=pending` et une vraie transaction simulée côté serveur, écran honnête "Commande transmise, paiement en attente" au lieu d'une fausse confirmation de paiement (`CMD-00030`, 1600 FCFA). Panier bien vidé côté serveur après chaque commande (0 article, `pharmacy=None`, vérifié en base les deux fois). Résolution du paiement simulé (écran Paiement) volontairement hors scope — c'est l'étape 7. **Note de reprise de session** : du code Checkout/Paiement non testé et non documenté traînait dans l'arbre de travail au démarrage de cette session (probablement une session précédente interrompue avant commit) ; mis de côté via `git stash` (récupérable, non supprimé) et le module a été réimplémenté intégralement à partir du contrat backend réel plutôt que repris tel quel.
7. [x] **Paiement simulé** — `POST /mobile/patient/payments/<reference>/simulate/resolve/`. Vérifié sur S8 le 21 août 2026 : nouvel écran `SimulatedPaymentScreen`, accessible depuis l'écran de succès du Checkout via un bouton "Simuler le paiement" quand une commande a un paiement en ligne en attente (COD n'en a pas besoin, déjà réglé à la réception). Ce n'est pas une simulation locale comme l'ancien `PaymentScreen` (70/30 aléatoire côté client) : le bouton appelle le vrai endpoint de résolution simulée exposé par le backend (aucune passerelle de paiement réelle branchée en dev). Trois issues testées et recoupées en base : paiement en attente non résolu (`CMD-00030`, `payment_status=pending`) ; paiement simulé réussi (`CMD-00031`, `payment_status=paid`, transaction `succeeded`) ; paiement simulé échoué (`CMD-00032`, `payment_status=failed`, transaction `failed`). Cas d'échec affiché honnêtement, sans fausse option de nouvelle tentative immédiate (la reprise de paiement est `POST .../retry-payment/`, prévue à l'étape 8 Commandes). Ancien `PaymentScreen` (simulation locale aléatoire) laissé intact et toujours utilisé par d'autres écrans encore en démo (retrait de commande, routes `/payment`/`/confirmation`) — pas de régression sur ces flux hors scope.
8. [x] **Commandes** — `GET /mobile/patient/orders/`, `GET /mobile/patient/orders/<id>/`, `POST .../cancel/`, `.../accept-changes/`, `.../reject-changes/`, `.../retry-payment/`. Vérifié sur S8 le 21 août 2026. Liste : filtres Tout/En attente/En cours/Livré/Annulé regroupant les 11 statuts réels de `Order.Status` en 4 familles visuelles (le libellé exact affiché sur chaque carte reste le `status_label` réel, granulaire), pagination "Charger plus". Détail : statut/paiement/récap/timeline 100% réels (timeline construite depuis `status_history`, plus aucune étape future inventée comme dans l'ancien mockup) ; adresse pharmacie honnête ("Adresse non renseignée" si vide), tags dérivés de `is_on_duty`/`is_24_7` (pas de distance factice) ; bouton "Appeler la pharmacie" retiré car l'endpoint commande n'expose pas de téléphone. **Mise à jour du 23 août 2026** : `phone` est désormais présent sur le champ `pharmacy` du payload commande (`_pharmacy_payload`) — le bouton "Appeler la pharmacie" peut être réintroduit sur l'écran de détail commande, pas encore fait côté app. Actions testées et recoupées en base : Annuler (`CMD-00033`, motif saisi, statut → `cancelled`) ; Reprendre le paiement (`CMD-00032`, réutilise `SimulatedPaymentScreen` du module 7 — nouvelle transaction simulée créée puis résolue avec succès, `payment_status` → `paid`) ; Accepter une proposition de modification (`CMD-00035`, statut → `accepted`, quantité et sous-total recalculés côté serveur, testé en simulant côté backend une proposition de la pharmacie via `propose_order_changes` puisque ce déclencheur est côté pharmacie, hors de portée de l'app patient). Refuser une proposition partage le même code qu'Annuler (`cancel_order_by_patient` côté backend, même dialogue de motif côté app) — non re-testé séparément. Bannière "commande en cours" de l'Accueil (déjà réelle depuis le module 1 via `PatientActiveOrder.id`) reliée pour la première fois au vrai détail de commande au lieu d'une route statique jamais branchée ; ancienne route `/order-detail` supprimée (plus d'usage sans id réel). **Bug trouvé et corrigé pendant la vérification** : le dialogue de saisie du motif (Annuler/Refuser) faisait planter l'app avec `_dependents.isEmpty is not true` — cause réelle (diagnostiquée via `flutter attach` + capture du VM service, le logcat classique étant noyé de bruit sur cet appareil) : le `TextEditingController` était disposé immédiatement après la fermeture du dialogue, alors que l'animation de sortie du dialogue reconstruisait encore le `TextField` une frame de plus ("A TextEditingController was used after being disposed."). Fix : disposer via `WidgetsBinding.instance.addPostFrameCallback` plutôt qu'immédiatement.
9. [x] **Assurance** — `GET /mobile/patient/insurance/insurers/`, `GET/POST/DELETE /mobile/patient/insurance/affiliation/` (pas de PATCH séparé côté backend : le POST fait un `update_or_create`, réutilisé pour la création comme pour la modification). Vérifié sur S8 le 23 août 2026 : sélection en cascade Assureur → Formule (chaque assureur a ses propres formules, ex. "Formule Essentielle" 60 %, "Formule Confort" 80 %), estimation de couverture recalculée depuis les vrais taux dérogatoires par catégorie du plan (`PlanCategoryRate`) au lieu du texte libre inventé du mockup. Création, modification et retrait tous confirmés en base (`Affiliation` créée/mise à jour/supprimée pour `patient.demo@gabpharma.ga`). **Gap de données découvert en session** : la base de démo n'avait qu'un seul assureur ("Mutuelle Santé Gabon") sans aucun plan actif — dropdown vide au premier test. Deux `Plan` de test créés manuellement via `manage.py shell` pour débloquer la vérification (Formule Essentielle 60 %, Formule Confort 80 % avec dérogations Hygiène 40 %/Contraception 50 %) ; à enrichir plus tard côté seed de démo si d'autres assureurs/plans sont nécessaires.
10. [x] **Notifications** — `GET /mobile/notifications/`. Vérifié sur S8 le 23 août 2026. Contrat backend découvert en session : liste **agrégée et calculée à la volée** (`"persistence": "aggregated"`, `"unread_count": null` explicites dans la réponse) construite uniquement depuis `OrderStatusHistory` du patient + commandes en attente proche de l'expiration — aucune table de notifications persistée, aucun événement "sécurité" ni "offres" côté serveur. Écarts assumés vs le mockup : filtres Tout/Commandes/Sécurité/Offres et la carte promo supprimés (rien à filtrer/afficher en réel, honnêteté plutôt que catégories vides) ; lu/non-lu reste local et éphémère (perdu à la fermeture de l'app, le serveur ne le persiste pas) ; ouvrir une notification navigue vers le vrai `OrderDetailScreen(orderId: target_id)` du module 8 au lieu des cibles factices (livraison/sécurité) de la démo. Mapping ajouté côté app : noms d'icônes Material renvoyés en snake_case par le backend (`hourglass_top`, `check_circle`, etc.) → `IconData`, et `tone` (amber/blue/green/red) → couleurs, regroupement par jour recalculé depuis le vrai timestamp. Bug UI trouvé et corrigé en session : le point de non-lu (repositionné sur l'icône plutôt que sur toute la carte) chevauchait le texte de l'heure et le masquait partiellement.
11. [x] **Support** — `GET /mobile/support/categories/`, `GET/POST /mobile/support/tickets/`, `GET /mobile/support/tickets/<id>/`, `POST .../reply/`. Vérifié sur S8 le 23 août 2026. Liste, création (sujet + catégorie réelle + message + commande liée parmi les vraies commandes du patient) et conversation tous branchés ; FAQ reste statique (l'API n'expose aucun contenu FAQ, ce n'est pas un gap). Écart assumé : pièce jointe laissée honnêtement "indisponible pour le moment" — l'API l'accepte réellement mais l'app n'a ni sélecteur de fichier ni client HTTP multipart (seulement du JSON pour l'instant) ; à construire séparément si besoin. **Découverte importante en session, contredit ce qui était noté avant branchement** : contrairement à l'ancienne conversation de démo, le backend réel **ne rouvre pas automatiquement un ticket résolu** quand le patient répond (`add_message` dans `apps/support/services.py` ne fait passer que `waiting_customer` → `in_progress` sur réponse patient ; `resolved`/`closed` restent inchangés). Seul le staff peut faire passer un ticket à "Résolu" (`TicketStaffUpdateForm`, dashboard support, permission `manage_support`) — le patient n'a jamais ce pouvoir. Bandeau de conversation corrigé en conséquence ("vous pouvez encore écrire, mais il ne se rouvre pas automatiquement") plutôt que d'afficher une fausse promesse de réouverture. Testé et confirmé en base : création de ticket (`SUP-1B782B77`, catégorie Livraison, lié à la commande #35), deux messages patient, résolution simulée côté staff (`update_ticket` via `manage.py shell`, aucun mécanisme patient pour le déclencher), puis un troisième message patient envoyé sur le ticket résolu — statut resté `resolved` en base, conforme au comportement réel.
12. [x] **Profil** — `GET/PATCH /mobile/profile/`, `POST /mobile/profile/password/`. Vérifié sur S8 le 23 août 2026. `ProfileScreen` (patient_shell.dart) converti en écran avec chargement réel (prénom/nom/e-mail réels au lieu de "Grâce Nziengui" codé en dur), `ProfileEditScreen` pré-rempli depuis le profil réel et sauvegardant via PATCH (prénom/nom/téléphone/nom d'utilisateur — e-mail volontairement en lecture seule, voir plus bas). Écran Sécurité : bandeau d'identité utilise désormais `AuthSession.currentUser` réel au lieu du nom/ville codés en dur ("Libreville, Gabon" supprimé, aucune donnée de ville n'existe côté profil). Changement de mot de passe branché sur `POST /mobile/profile/password/` ; confirmé par un canal indépendant du serveur (e-mail réel de confirmation reçu, avec la mention "si ce n'est pas vous, contactez le support"). **E-mail volontairement non modifiable depuis l'app** : recherche approfondie du backend confirmant que ce n'est pas un gap du branchement mais une restriction voulue — le seul flux de changement d'e-mail du code (`profile_email_change_request/confirm`, dashboard web) est réservé au Super Administrateur (`PermissionDenied` pour tout autre rôle) ; aucun chemin de changement d'e-mail n'existe pour un patient, ni web ni mobile. **Correction du 23 août 2026, suite à une question de l'utilisateur sur `propositions_backend.md`** : relecture plus fine du code — ce n'est probablement pas une décision de sécurité délibérée. Le flux est réservé au Super Administrateur alors que `_can_edit_own_profile` (déjà utilisé pour le reste du profil, y compris le mot de passe) autorise tous les rôles actifs, et `EmailChangeRequestForm` est déjà générique (mot de passe actuel + OTP à la nouvelle adresse + notification des deux adresses + audit log) — un niveau de soin qui ne correspond pas à une simple restriction voulue. Lecture la plus probable : flux resté attaché à l'écran de paramètres du Super Admin et jamais généralisé. Reclassé comme proposition d'ajout dans `propositions_backend.md` (point 1.4) plutôt que comme décision actée. **Mise à jour du 23 août 2026 : implémenté côté backend.** La garde web `_is_super_admin` a été remplacée par `_can_edit_own_profile` (tous rôles actifs), et deux nouveaux endpoints mobiles génériques existent : `POST /mobile/profile/email-change/` (mot de passe actuel + nouvel e-mail → envoie un code à la nouvelle adresse) et `POST /mobile/profile/email-change/verify/` (code à 6 chiffres → applique le changement, notifie les deux adresses, invalide les sessions web). Pas encore branché côté app : `ProfileEditScreen` affiche toujours l'e-mail en lecture seule — il faut un sous-écran dédié (mot de passe + nouvel e-mail, puis code), sur le modèle des écrans de vérification déjà construits pour l'inscription/mot de passe oublié. **Bug trouvé et corrigé pendant la vérification** : même famille de bug que celui déjà documenté au module 8 (`_dependents.isEmpty is not true`) — un 401 (expiration du token JWT à 20 min, gap déjà connu) déclenchait la déconnexion globale automatique pendant qu'un formulaire en feuille modale (changement de mot de passe, et création de ticket support au module 11) essayait encore de mettre à jour son propre état sur un écran en cours de démontage, provoquant un crash au lieu du retour propre à l'écran de connexion. Fix : vérifier `context.mounted` avant tout `setState`/`ScaffoldMessenger.of(context)` dans les deux formulaires concernés. **Mot de passe du compte de démo changé pendant ce test** : `demonstration` → `Demo1987.` — mettre à jour toute référence à l'ancien mot de passe pour les prochaines sessions de test.
13. [x] **Inscription** — `POST /mobile/auth/register/`, `POST /mobile/auth/register/verify/`. Vérifié sur S8 le 23 août 2026 : compte réel créé et activé en base (`Kylian MBAPPE`, `kylian.mbappe@gmail.com`, statut `pending → active` confirmé via `manage.py shell`), mot de passe validé par les vrais validateurs Django (longueur/similarité/mot de passe courant/tout-numérique) au lieu de l'ancienne jauge de force maison côté client (la jauge visuelle reste affichée à titre indicatif, mais ne bloque plus la soumission — seule la vraie erreur backend le fait). Nouvel écran `RegisterVerifyScreen` (route `/register-verify`) : code à 6 chiffres, connexion automatique après activation (le endpoint verify renvoie directement `access`/`refresh`, comme la vérification 2FA du login). **Gap découvert et assumé (au 23 août 2026 avant correction)** : `/mobile/auth/resend-2fa/` filtre en dur `purpose=LOGIN_2FA` côté serveur, donc il ne peut jamais retrouver un code d'inscription (`purpose=REGISTRATION`) même si `/register/` répond `can_resend: true` ; et se réinscrire avec le même e-mail échoue aussi (`clean_email` bloque sur un compte existant quel que soit son statut, y compris `pending`). Donc si le code à 6 chiffres (valable 5 min) expire ou est perdu, l'utilisateur était bloqué sans recours dans l'app — il fallait une intervention admin (supprimer le compte `pending` en base) ou un développement backend séparé. Pas de bouton "Renvoyer" câblé sur cet écran en conséquence (aurait échoué silencieusement) ; message honnête affiché à la place.

**Les deux corrigés côté backend le 23 août 2026 :**
- `/mobile/auth/resend-2fa/` accepte désormais n'importe quel `purpose` en cours (`LOGIN_2FA`, `REGISTRATION`, `PASSWORD_RESET`), plus seulement la 2FA de connexion — un bouton "Renvoyer" peut maintenant être câblé sur `RegisterVerifyScreen` en réutilisant le même appel que `VerifyScreen`.
- Se réinscrire avec le même e-mail/téléphone **reprend désormais le compte `pending` existant** au lieu d'échouer, tant que ce compte n'a jamais été activé (`role=PATIENT`, `status=PENDING` uniquement — un compte actif/suspendu/rejeté/désactivé, ou d'un autre rôle, reste bloqué comme avant). L'ancien code abandonné est invalidé automatiquement, un nouveau code est émis. **Aucun changement requis côté app** pour ce deuxième point : si l'app rappelle simplement `POST /register/` avec les mêmes informations, ça fonctionne déjà — seul le bouton "Renvoyer" (premier point) nécessite du câblage Flutter. **Deux bugs annexes trouvés et corrigés pendant la vérification, hors scope strict du module mais découverts via le test avec un vrai compte distinct du compte de démo** : (1) l'avatar de `PatientTopBar` (widgets.dart) affichait les initiales "GN" codées en dur pour n'importe quel utilisateur connecté au lieu de dériver les vraies initiales depuis `AuthSession.currentUser` — invisible jusqu'ici car le compte de démo s'appelle justement Grâce Nziengui ; corrigé en ajoutant `AuthUser.initials` (même logique que `PatientProfile.initials` déjà existant) et en l'utilisant dans la barre du haut. (2) Absence d'e-mail de bienvenue après activation : **pas un bug**, vérifié dans `apps/accounts/emails.py` — `send_registration_received` existe mais est réservé aux inscriptions professionnelles (pharmacie/livreur), aucun e-mail de bienvenue n'est prévu côté backend pour un patient au-delà de l'e-mail contenant le code d'activation ("Activez votre compte Gab'Pharma").
14. [x] **Mot de passe oublié** — `POST /mobile/auth/password-reset/`, `/verify/`, `/confirm/`. Vérifié sur S8 le 23 août 2026 : parcours complet avec le compte `kylian.mbappe@gmail.com`, recoupé en base via `AuthenticationEvent` (`code_sent` → `code_verified` → `password_changed`, tous `purpose=password_reset`). Pas de connexion automatique après réinitialisation (le backend ne renvoie pas de tokens sur `/confirm/`, juste un `detail` — "Reconnectez-vous"), conforme à l'écran de succès déjà existant. Jauge de mot de passe locale ("Une majuscule et un chiffre", "Un caractère spécial") gardée à titre de recommandation visuelle uniquement mais ne bloque plus la soumission — seule la longueur ≥8 caractères est vérifiée côté client, le reste (similarité au nom/e-mail, mot de passe courant, tout numérique) est laissé aux vrais validateurs Django et leurs messages d'erreur réels affichés tels quels (même principe que pour l'inscription, module 13). **Piège identifié et évité en lisant le code avant d'implémenter** : `MobilePasswordResetRequestView` répond toujours 200 (anti-énumération de comptes), y compris quand la limite de renvoi (`AUTH_CODE_RESEND_COOLDOWN_SECONDS=60`) est atteinte — dans ce cas précis, il avale l'exception `CodeRateLimited` et renvoie un `challenge_id` factice (`uuid4()` aléatoire) qui ne validera jamais aucun code. Renvoyer un code trop tôt aurait donc silencieusement cassé le code déjà reçu et encore valide. Contre-mesure choisie : contrairement à l'inscription (aucun renvoi possible), ici un vrai renvoi fonctionne (ré-appeler `/password-reset/` avec le même identifiant régénère un vrai code), mais le bouton "Renvoyer le code" reste désactivé pendant 60 secondes après chaque envoi (compte à rebours), pour ne jamais retomber dans la fenêtre de cooldown qui déclenche le challenge factice — même principe de prudence que le compte à rebours de renvoi déjà en place sur `VerifyScreen` (login 2FA), mais calé sur la vraie valeur de cooldown backend plutôt que sur les 5 minutes de validité du code.
15. [ ] **Notifications push (token FCM)** — **débloqué côté backend le 23 août 2026**, rien encore fait côté app. Historique : `push_notification_service.dart` récupère déjà un token FCM localement (`// TODO` explicite dans le code), mais jusqu'ici aucun endpoint backend n'existait pour l'enregistrer.
    Désormais disponibles : `POST /mobile/devices/` (upsert par `push_token` — body `{"push_token": "...", "platform": "android", "device_label": "..."}`, réutilisable si le token change de compte sur le même appareil), `POST /mobile/devices/unregister/` (à appeler à la déconnexion), `GET /mobile/profile/devices/` (liste des appareils enregistrés pour ce compte). L'envoi réel de notifications est aussi opérationnel côté serveur (credentials Firebase Admin configurées, déclenché sur changement de statut de commande/livraison et sur les tickets support) — reste uniquement à appeler `POST /mobile/devices/` avec le token FCM déjà récupéré localement, probablement juste après la connexion réussie et à chaque démarrage si le token a changé.

## Nouvelles capacités backend (23 août 2026), pas encore rattachées à un module ci-dessus

Trouvé côté backend en croisant ce fichier avec `documentation/api_besoins_patient_evolutions_20260823.md` du dépôt Django. Aucune de ces trois capacités ne correspond à un écran déjà branché dans la liste 1-15 ci-dessus — à rattacher à un module existant ou à en créer un nouveau, selon ce que l'app a réellement comme écran pour chacune :

`GET /mobile/patient/pharmacies/`, `GET /mobile/patient/payments/` et `POST /mobile/profile/deactivate/` (listés ici initialement) sont désormais tous rattachés — voir les sections "Session du 24 août 2026" et "Session du 25 août 2026" ci-dessous.

## Session du 24 août 2026 — items "moyens" de `propositions_backend.md`

- [x] **Changement d'e-mail** — `POST /mobile/profile/email-change/` + `/verify/`. Nouvel écran `EmailChangeScreen` (patient_shell.dart), à deux étapes (mot de passe + nouvel e-mail, puis code à 6 chiffres), calqué sur `RegisterVerifyScreen`. Câblé depuis `ProfileEditScreen` : le champ e-mail affiche un bouton "Modifier". Vérifié sur S8 le 24 août 2026 avec le compte de démo, changé vers `patient.demo@gmail.com` (code reçu et vérifié via MailHog). **Bug trouvé et corrigé en session** : le champ e-mail était un `TextField(enabled: false)` avec le bouton "Modifier" en `suffixIcon` — Flutter enveloppe un champ désactivé dans un `IgnorePointer` qui couvre aussi son `suffixIcon`, rendant le bouton visible mais totalement inerte. Remplacé par un `Container` non désactivé qui reproduit le style d'un champ (bordure `outlineVariant`, radius 14px) plutôt qu'un vrai `TextField` désactivé — piège à retenir pour tout futur champ "lecture seule + action" dans l'app. **Identifiant de connexion du compte de démo à jour depuis ce test** : `patient.demo@gmail.com` (l'ancien `patient.demo@gabpharma.ga` ne fonctionne plus) — mot de passe inchangé (`Demo1987.`), le pré-remplissage de l'écran de connexion (`auth_screens.dart`) reste à mettre à jour si besoin.
- [x] **Désactivation de compte** — `POST /mobile/profile/deactivate/`. Action ajoutée dans `SecurityScreen` (détail_screens.dart), section "Zone sensible", confirmation par mot de passe. Vérifié sur S8 le 24 août 2026. **Bug trouvé et corrigé en session** : la boîte de dialogue de confirmation (`AlertDialog` avec avertissement + champ mot de passe) débordait de 97px quand le clavier s'ouvrait — `AlertDialog` sans `scrollable: true` ne rétrécit pas son contenu à l'ouverture du clavier. Fix : `scrollable: true` sur cet `AlertDialog`. Réactivation par simple reconnexion + 2FA également confirmée (reconnexion réussie avec `patient.demo@gmail.com` / `Demo1987.` après désactivation).
- [x] **Historique paiements/remboursements** — `GET /mobile/patient/payments/`. `PaymentsHistoryScreen` (détail_screens.dart) converti en `StatefulWidget` avec chargement réel, pagination "Charger plus", statut de remboursement dérivé de `order.payment_status` (le backend ne modélise pas les remboursements comme des transactions séparées). Vérifié sur S8 le 24 août 2026. **Bug de mise en page trouvé et corrigé en session** : `_TransactionCard` empilait nom de pharmacie + badge de statut sur la même ligne (`Row` avec un `Expanded` d'un côté et une colonne montant+badge non contrainte de l'autre) — avec un badge "REMBOURSEMENT EN COURS" (donnée réelle, absente des données de démo d'origine), le badge poussait le nom de pharmacie qui se retrouvait tronqué/coupé au milieu d'un mot. Carte restructurée en 3 lignes empilées (nom de pharmacie + référence ; date + montant ; badge de statut seul en pleine largeur) : plus jamais deux blocs de texte de longueur variable ne se disputent la largeur sur une même ligne, donc rien ne peut plus se faire couper quelle que soit la longueur du nom de pharmacie ou du libellé de statut.
- [x] **Notifications push FCM** — `POST /mobile/devices/` / `/unregister/`. Token envoyé après connexion/inscription/restauration de session, désenregistré à la déconnexion/désactivation ; tap sur une notification "commande" ou "ticket support" navigue vers le bon écran (câblé via un callback `PushNotificationService.onNotificationTap` réglé dans `main.dart`, pour ne pas faire dépendre `core/` des écrans). Vérifié de bout en bout sur S8 le 24 août 2026 : enregistrement confirmé en base (`Device` actif pour `patient.demo@gmail.com`, token FCM réel) ; push réel déclenché en changeant le statut d'une commande de démo (`apps.orders.services._set_status`, commande #35 accepted → preparing) — notification reçue dans le tiroir Android app en arrière-plan, tap dessus a bien ouvert `OrderDetailScreen` sur la commande #00035. Chemin "support_ticket" (`ConversationScreen`) câblé de façon identique mais non testé séparément (même mécanisme, `_notify_requester` côté backend). **Chemin "delivery" branché et vérifié le 7 septembre 2026** (voir section "Session du 7 septembre 2026" ci-dessous) — navigue vers `DeliveryTrackingScreen(orderId: ...)`, sur le même modèle que "order"/"support_ticket".

## Session du 25 août 2026 — items "lourds" de `propositions_backend.md`

- [x] **Géolocalisation patient réelle** — package `geolocator` (Android : `ACCESS_FINE_LOCATION`/`ACCESS_COARSE_LOCATION` dans `AndroidManifest.xml`). Nouveau `PatientLocationService` (`lib/src/core/location_service.dart`) : demande la permission runtime et met en cache la position en mémoire pour la session (TTL 5 min), ne lève jamais d'exception (repli `null` honnête si permission refusée/service coupé/timeout). `lat`/`lng` transmis sur `fetchCatalog`, `fetchStockDetail`, `fetchPharmacyDetail` et le nouveau `fetchPharmacies` (voir point suivant) ; `distance_km` déjà renvoyé par l'API (Haversine serveur) affiché partout où une carte pharmacie apparaît (`formatDistanceKm` : "850 m" sous 1 km, "1,2 km" au-delà). **Stratégie de permission choisie** : jamais de prompt "surprise" sur un écran passif (détail médicament/pharmacie utilisent seulement `PatientLocationService.cachedPosition`, un getter synchrone qui ne déclenche rien) — seule une action explicite ("Proximité" en Recherche et sur l'écran Pharmacies) appelle `currentPosition()` et déclenche le vrai prompt runtime. Écran Recherche : le bouton "Proximité" (déjà présent visuellement depuis le lot 2 avec le message honnête "à venir", voir `CLAUDE.md`) est maintenant réellement câblé — tri client-side par `distance_km` (l'API catalogue calcule la distance par ligne mais ne trie pas dessus, contrairement à `/pharmacies/` qui trie déjà côté serveur si `lat`/`lng` fournis). Vérifié sur S8 le 25 août 2026 : prompt de permission Android affiché et accepté, distance réelle affichée et cohérente entre l'écran Recherche (1,8 km / 2,1 km), l'écran Pharmacies et le détail pharmacie (même position mise en cache réutilisée sans re-prompt).
- [x] **Écran "Parcourir les pharmacies"** — nouveau `PharmacyBrowseScreen` (`detail_screens.dart`, route `/pharmacies`), backé par `GET /mobile/patient/pharmacies/` (`?q=`, `?zone=`, `?on_duty=true`, `?lat=&lng=`), pagination "Charger plus" identique aux autres listes. Recherche par nom (debounce 400ms), chips de zone, chip "De garde maintenant", chip "Proximité" (déclenche `currentPosition()` et re-trie). **Placement dans la nav, décidé avec l'utilisateur avant implémentation** : pas de nouvel onglet (le bottom nav à 5 onglets reste figé, fidèle à la spec des 24 écrans) — le bouton "Voir tout" de la section "Pharmacies à proximité" de l'Accueil (`PatientHomeScreen`), qui pointait par erreur vers l'onglet Recherche (médicaments, pas pharmacies) depuis le lot 2, a été corrigé pour pousser ce nouvel écran à la place. Vérifié sur S8 le 25 août 2026 : liste réelle (Pharmacie NOUO Cecile, Pharmacie du Centre), filtres zone/garde fonctionnels, tri par proximité confirmé (résultats re-triés par `distance_km` croissant après acceptation du prompt de localisation).
- [x] **Biométrie locale (Touch ID / Face ID)** — package `local_auth`, verrou purement local sans aucun changement backend (version minimale actée avec l'utilisateur, pas de registre "appareil de confiance" côté serveur — voir `propositions_backend.md` §6). `MainActivity.kt` : `FlutterActivity` → `FlutterFragmentActivity` (requis par `local_auth` sur Android, le plugin s'appuie sur `androidx.fragment`). Nouveau `BiometricLockService` (`lib/src/core/biometric_lock_service.dart`) : préférence `biometric_lock_enabled` persistée dans `flutter_secure_storage` (même mécanisme que les tokens), `authenticate()` ne lève jamais d'exception (repli `false`, jamais de blocage si le capteur devient indisponible entre-temps). Le toggle "Touch ID / Face ID" de `SecurityScreen` — jusqu'ici factice avec le message honnête "sera activé une fois le capteur connecté" (voir `CLAUDE.md`) — est désormais réel : détecte le support matériel au chargement (`canCheckBiometrics` + `isDeviceSupported`), désactivé avec message honnête si aucun capteur, et l'activation exige une authentification biométrique de confirmation avant de persister la préférence (pas de simple flip de switch). `SplashScreen` : après restauration de session réussie, si le verrou est activé ET le capteur toujours disponible, affiche un écran de verrouillage dédié (icône empreinte, bouton "Réessayer" si échec/annulation) et attend une authentification réussie avant de naviguer vers `/home` — sinon repli honnête (accès direct) pour ne jamais bloquer l'utilisateur si le capteur a disparu entre-temps (téléphone reconfiguré, empreintes effacées). Vérifié sur S8 le 25 août 2026 : activation du toggle a déclenché le vrai `BiometricPrompt` natif Android (avec repli "Utiliser un mot de passe" puisque `biometricOnly: false`), préférence persistée après relance de l'app. Redémarrage à froid de l'app (force-stop + relance, y compris après un verrouillage/déverrouillage complet du téléphone) confirmé via logcat (`FingerprintService`/`BiometricPrompt`) : le prompt natif s'affiche bien à chaque lancement et exige une lecture d'empreinte réelle (`onAuthenticated`) avant que l'app ne poursuive vers l'Accueil — aucun contournement possible sans capteur.
    - **Non couvert par cette version minimale, assumé** : pas de re-verrouillage au retour d'arrière-plan (l'app ne verrouille qu'au lancement à froid/restauration de session, pas à chaque reprise depuis le multitâche) — cohérent avec la portée demandée ("protège l'accès au token déjà stocké", pas un vrai second facteur applicatif) ; à construire séparément (observer `AppLifecycleState` au niveau de `PatientShell`) si demandé explicitement plus tard.
- **Piège de session rencontré, sans lien avec le code applicatif** : le tunnel `adb reverse tcp:8004 tcp:8004` s'est révélé se couper plus souvent que d'habitude pendant cette session (reconnexions USB liées aux tests de verrouillage/déverrouillage physique de l'écran pour valider la biométrie) — à re-vérifier (`adb reverse --list`) après chaque cycle verrouillage/déverrouillage de l'écran, pas seulement après une reconnexion USB franche.

## Session du 4 septembre 2026 — position temps réel du livreur (écran 17, Suivi de livraison)

- [x] **Suivi de livraison en temps réel** — `GET /mobile/patient/orders/<id>/` (bloc `delivery`, déjà exposé côté backend depuis la session du même jour côté dépôt `gabpharma_livreur` — voir `documentation/api_contrat.md` §6 et `documentation/cadrage_suivi_temps_reel_livreur.md` du dépôt Django). Jusqu'ici `DeliveryTrackingScreen` (écran 17) était entièrement déconnecté : `StatelessWidget` sans paramètre réel, poussé uniquement (et à tort) depuis l'ancien écran de confirmation démo (`OrderConfirmationScreen`, lui-même mort depuis le branchement du vrai Checkout au module 6), toujours sur données statiques (`_deliveryTrackingDefault`). Travail de cette session :
  - Nouveau modèle `PatientDelivery` (`patient_catalog.dart`) parsant le bloc `delivery` (statut, nom/téléphone du livreur, `is_late`, `eta`, et les trois champs de position `courier_latitude`/`courier_longitude`/`courier_position_updated_at`) ; `PatientOrder.delivery` (nullable) et `PatientOrder.deliveryAddress` ajoutés en parallèle.
  - `DeliveryTrackingScreen` converti en `StatefulWidget` prenant un vrai `orderId`, chargé via `fetchOrderDetail` avec un `Timer.periodic` de 20s (le livreur ping toutes les 45s côté app Livreur — 20s reste réactif sans matraquer l'API) tant que l'écran est ouvert.
  - Carte : remplace le fond illustratif par un vrai `GoogleMap` (`liteModeEnabled`, même pattern que le mini-aperçu de l'écran Détail pharmacie) avec un marker sur la position du livreur **uniquement** si `courier_latitude`/`courier_longitude` sont non nuls (le backend les met déjà à `null` si la dernière position a plus de 5 minutes — jamais de position périmée affichée comme "en direct" par construction). Sinon, fond illustratif dégradé conservé avec un message honnête ("Position du livreur non disponible pour le moment — elle apparaît automatiquement dès qu'il est en route.") plutôt qu'un faux marker centré par défaut.
  - Timeline de suivi (`PROGRESSION`) rebranchée sur `order.statusHistory` en extrayant la logique déjà utilisée par le Détail commande (module 8) dans une fonction top-level partagée (`_orderTimelineSteps`) au lieu de dupliquer une liste d'étapes fictives.
  - Cartes "Pharmacie d'origine"/"Destination" rebranchées sur les vraies données de la commande (`order.pharmacyName`/`pharmacyAddress`, `order.deliveryAddress`/`deliveryModeLabel`) ; rating/nombre de courses du livreur (fictifs, aucun champ backend équivalent) retirés et remplacés par le statut réel de la livraison (avec libellé "En retard" en rouge si `is_late`).
  - Bouton d'appel du livreur rebranché sur `callPhoneNumber` (le vrai helper `tel:` déjà utilisé pour la pharmacie, ajouté au projet après la construction initiale de cet écran) au lieu du message "indisponible en démonstration" — honnête si le numéro est vide.
  - Nouveau point d'entrée réel : `OrderDetailScreen` (module 8), bloc `_inDeliveryStatuses` (statuts `awaiting_courier`/`in_delivery`) — le message "pas encore branché à l'API" est remplacé par un bouton "Suivre la livraison en temps réel" qui pousse `DeliveryTrackingScreen(orderId: ...)`. L'ancien bouton "Suivre ma commande" de `OrderConfirmationScreen` (chemin mort, aucun `orderId` disponible à cet endroit) affiche désormais un message honnête au lieu de naviguer vers une route qui n'existait plus ; route nommée `/delivery` supprimée de `app.dart` (plus jamais construite sans id réel, même principe que `OrderDetailScreen` qui n'a jamais eu de route nommée).
  - **Vérifié de bout en bout sur S8 physique le 4 septembre 2026**, avec l'app Livreur (`gabpharma_livreur`) et l'app Patient en parallèle sur le même appareil : commande `CMD-00023` (compte démo `active-carine-demo@example.ga`, livreur Carine Mba), position figée (>5 min) au premier chargement → fallback illustratif honnête confirmé avec le bon message ; position rafraîchie manuellement en base pour simuler un ping frais (même effet qu'un vrai ping de l'app Livreur, mécanisme de ping déjà vérifié séparément le jour même, voir `branchement_livreur.md` §5) → la carte réelle est apparue automatiquement au tick suivant du timer (~20s), marker positionné correctement, légende passée à "Position du livreur en direct.", statut "En livraison • En retard" affiché en rouge (donnée réelle, `delivery_deadline` dépassé), ETA et pharmacie/destination réels, aucun overflow constaté à aucun moment.
  - **Suite donnée le 7 septembre 2026** : le type de notification push `delivery` (voir module "Notifications push FCM" ci-dessus), qui n'avait jusqu'ici aucune navigation ciblée, pointe désormais vers `DeliveryTrackingScreen` — voir section "Session du 7 septembre 2026" ci-dessous.

## Session du 7 septembre 2026 — navigation ciblée sur la notification push "delivery"

- [x] **Notification push "delivery" → `DeliveryTrackingScreen`** — dernier chemin de `send_push_to_user` sans navigation ciblée (voir module "Notifications push FCM" et note du 4 septembre 2026 ci-dessus). Payload backend (`apps/deliveries/services.py`, `_notify_patient_of_delivery_status`) complété avec `order_id` (jusqu'ici seul `delivery_id` était envoyé, inutilisable côté app patient qui identifie tout par `order_id`). `main.dart` (`_handlePushNotificationTap`) : nouveau `case 'delivery'` sur le même modèle que `'order'`/`'support_ticket'` — lit `data['order_id']`, ignore silencieusement si absent/invalide (`int.tryParse`, pas de crash), pousse `DeliveryTrackingScreen(orderId: orderId)`. Commentaire au-dessus de la fonction mis à jour en conséquence.
  - **Vérifié de bout en bout sur S8 physique le 7 septembre 2026**, avec un scénario construit en base (`manage.py shell`, services réels `create_delivery_for_order`/`assign_delivery`/`pick_up_delivery`/`complete_delivery`, pas de contournement direct des modèles) : commande #36 (patient `patient.demo@gmail.com`), livreur `livreur-pharmacie-du-centre@example.ga`, zone Libreville. Push "Récupérée" (assigned → picked_up) et push "Livrée" (picked_up → delivered, code de remise réel récupéré via l'API MailHog) tous deux reçus dans le tiroir Android app en arrière-plan. Tap sur la notification "Livrée" a ouvert `DeliveryTrackingScreen` sur `Commande #CMD-00036` (livreur "Serge Ekomy", statut "En livraison", timeline à jour), aucun overflow constaté.
  - **Piège de session rencontré (déjà documenté ailleurs dans ce fichier, reconfirmé ici)** : le tunnel `adb reverse tcp:8004 tcp:8004` s'était coupé entre le build/install de l'app et le test — premier chargement de l'écran affichait "Impossible de joindre l'API Gab'Pharma" malgré une navigation par ailleurs correcte ; `adb reverse` rétabli puis "Réessayer" a chargé les vraies données.
  - **Piège d'automatisation ADB rencontré** : taper une notification via `input tap` sur le tiroir Samsung One UI a été peu fiable dans cette session (deux tentatives sans effet visible malgré des coordonnées dans les bounds `uiautomator dump` du nœud cliquable) avant qu'un troisième essai ne fonctionne, sans changement de méthode identifiable — les logs `WindowManager`/`ActivityManager` (`adb logcat`, filtré sur `MainActivity`/`WindowFocus`) sont le seul moyen fiable de confirmer qu'un tap a réellement ramené l'app au premier plan, une capture d'écran seule pouvant rester ambiguë si le tiroir de notifications reste affiché par-dessus.

## Méthode de vérification pour chaque module

1. Brancher le code Flutter sur l'endpoint réel (remplacer les données locales par un appel `ApiClient`).
2. Rebuild l'APK debug : `flutter build apk --debug --dart-define=API_BASE_URL=http://127.0.0.1:8004/api/v1/ --dart-define=DEMO_MODE=false`
3. `adb reverse tcp:8004 tcp:8004` (le tunnel ne survit pas à une reconnexion USB — à refaire si le S8 se déconnecte)
4. Installer et tester sur le S8 physique.
5. Vérifier le log du serveur Django (pas juste que l'écran s'affiche) pour confirmer un `200` et pas une erreur silencieuse côté client.
6. Cocher la case ci-dessus une fois vérifié.

## Livreur (hors scope de ce fichier)

Aucun branchement commencé côté Livreur — voir `gabpharma_livreur/CLAUDE.md`. Le backend est déjà prêt côté Livreur aussi (`/mobile/courier/summary/`, `/availability/`, `/deliveries/...`, `/ledger/`) mais rien n'est câblé côté Flutter. À traiter séparément une fois ce fichier soldé, ou en parallèle si décidé plus tard.
